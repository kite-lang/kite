main = -> {

     one = 1
     
     incre = |x| -> { x + one }

     incre_twice = |x| -> {
                 one = 2
                 x + one
     }

     print(one)
     print(incre(0))
     print(incre_twice(0))
}